import { TOOL_TYPES } from '../../const'


class SCALEBAR {
  constructor(options) {
    this.toolType = TOOL_TYPES.SCALEBAR;
    this.active = false;
    this.visible = false;
  }
  render (e) {

  }
}

export default SCALEBAR;